public class perulangan2 {

    public static void main(String[] args) {

        String[] nama = {
            "Asep", "Budi", "Joko", "Zaenal"
        };

        for (int i = 0; i < nama.length; i++) {            
            if (nama[i].   ("Asep") || nama[i].equals("Zaenal")) {
                System.out.println(nama[i]);
            }
        }
    }
}
